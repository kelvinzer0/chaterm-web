RENDERER_NODE_ENV=development
RENDERER_APP_EDITION=cn

# API Endpoints (default to CN for development)
RENDERER_VUE_APP_API_BASEURL=/api
RENDERER_KMS_SERVER_URL=/api
RENDERER_SYNC_SERVER_URL=/api
RENDERER_VUE_APP_PROXXY=true

# Voice / Docs / SSO (default to CN for development)
RENDERER_SPEECH_WS_URL=/api/speech/asr
RENDERER_DOCS_BASE_URL=/docs
RENDERER_SSO=/sso

# Defaults
RENDERER_DEFAULT_LANGUAGE=zh-CN
